// const grid = document.querySelectorAll('list-body_grid');
// grid.forEach(grid => {
//   animateCSSGrid.wrapGrid(grid, {duration : 600});
// })

// grid.style.width = '500px'
// forceGridAnimation()